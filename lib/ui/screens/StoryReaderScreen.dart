import 'package:flutter/material.dart';

class StoryReaderScreen extends StatelessWidget {
  const StoryReaderScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("The boy and the dog"),
        centerTitle: true,
      ),
    );
  }
}
